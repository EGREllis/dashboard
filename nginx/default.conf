server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    #access_log  /var/log/nginx/host.access.log  main;

    location / {
        proxy_pass	http://frontend-react:3000/;
    }

    location /api {
	    proxy_pass	http://users-springboot:8080/;
    }
}
